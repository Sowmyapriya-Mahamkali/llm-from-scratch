# Experiments

- Visualize attention by instrumenting `model` to return attention maps.
- Plot training loss using the logs printed by trainer.
- Try different tokenizers, or use HuggingFace `tokenizers` for speed.
